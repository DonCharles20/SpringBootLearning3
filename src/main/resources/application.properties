spring.application.name=springweb
# server.port=8081 <---how to change port numbers
spring.datasource.url=jdbc:h2:mem:mydb
spring.datasource.driver-class-name=org.h2.Driver
# use /h2-console to access the H2 console using your port number"Localhost:8080/h2-console"

# The line below shows sql queries in the console
spring.jpa.show-sql=true
spring.dataSource.username=don
spring.dataSource.password=mydb
